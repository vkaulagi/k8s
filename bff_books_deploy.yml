apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-books
  namespace: book-store-ns
spec:
  selector:
    matchLabels:
      run: bff-books
  replicas: 1
  template:
    metadata:
      labels:
        run: bff-books
    spec:
      containers:
      - name: bff-books-container
        image: vkaulagi/bff_books
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /status
            port: 80
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3